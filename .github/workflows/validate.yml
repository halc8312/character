name: Validate Characters and Locations

on:
  push:
    branches: [ main, master ]
    paths:
      - 'characters/**'
      - 'locations/**'
      - 'maps/**'
      - 'links/**'
      - 'schemas/**'
      - 'scripts/**'
      - 'relations/**'
  pull_request:
    branches: [ main, master ]
    paths:
      - 'characters/**'
      - 'locations/**'
      - 'maps/**'
      - 'links/**'
      - 'schemas/**'
      - 'scripts/**'
      - 'relations/**'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyyaml jsonschema
      
      - name: Run character validation
        run: python scripts/validate_characters.py
      
      - name: Run location validation
        run: python scripts/validate_locations.py
